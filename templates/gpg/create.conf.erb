          %echo Generating a basic OpenPGP key
          Key-Type: DSA
          Key-Length: 1024
          Subkey-Type: ELG-E
          Subkey-Length: 1024
          Name-Real: <%= @name %>
          Name-Comment: generated by puppet
          Name-Email: <%= @email %>
          Expire-Date: 0
          Passphrase: <%= @passphrase %>
          %pubring <%= scope['duplicity::duply_key_dir'] %>/keys.pub
          %secring <%= scope['duplicity::duply_key_dir'] %>/keys.sec
          # Do a commit here, so that we can later print "done" :-)
          %commit
          %echo done